import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";

test("ships the Routine EASY product instead of starter content", async () => {
  const [page, layout, packageJson, routinesRoute, dataRoute, instructionImageRoute, quantityRoute, completionRoute, storage, deviceStorage, mobileIndex, mobileMain, capacitorConfig] = await Promise.all([
    readFile(new URL("../app/page.tsx", import.meta.url), "utf8"),
    readFile(new URL("../app/layout.tsx", import.meta.url), "utf8"),
    readFile(new URL("../package.json", import.meta.url), "utf8"),
    readFile(new URL("../app/api/routines/route.ts", import.meta.url), "utf8"),
    readFile(new URL("../app/api/data/route.ts", import.meta.url), "utf8"),
    readFile(new URL("../app/api/instruction-image/route.ts", import.meta.url), "utf8"),
    readFile(new URL("../app/api/quantity-completions/route.ts", import.meta.url), "utf8"),
    readFile(new URL("../app/api/completions/route.ts", import.meta.url), "utf8"),
    readFile(new URL("../db/storage.ts", import.meta.url), "utf8"),
    readFile(new URL("../app/device-storage.ts", import.meta.url), "utf8"),
    readFile(new URL("../mobile/index.html", import.meta.url), "utf8"),
    readFile(new URL("../mobile/main.tsx", import.meta.url), "utf8"),
    readFile(new URL("../capacitor.config.ts", import.meta.url), "utf8"),
  ]);

  assert.doesNotMatch(page, /Today’s routines/);
  assert.match(page, /Calendar/);
  assert.match(page, /Add a routine/);
  assert.match(page, /Your routines start here/);
  assert.match(page, /Create one small routine and build from there/);
  assert.match(page, /className="routines-empty"/);
  assert.match(page, /Arrange your day/);
  assert.match(page, /function RoutineArrangement/);
  assert.match(page, /draggable onDragStart/);
  assert.match(page, /Time section for \$\{routine\.name\}/);
  assert.match(page, /Auto \(\{automaticLabel\}\)/);
  assert.match(page, /const TIME_SECTIONS/);
  assert.match(page, /resolvedRoutineSection/);
  assert.match(page, /today-time-groups/);
  assert.doesNotMatch(page, /function OnboardingPage|routineez-onboarding-complete/);
  assert.match(page, /SPLASH_DURATION_MS = 2100/);
  assert.match(page, /splash-routine/);
  assert.match(page, /splash-blobs/);
  assert.match(page, /app-background-blobs/);
  assert.match(page, /function BlobCorners/);
  assert.match(page, /splashOverlay/);
  assert.match(page, /SPLASH_FADE_MS = 650/);
  assert.match(page, /splash-blob-purple/);
  assert.match(page, /splash-blob-coral/);
  assert.match(page, /splash-blob-gold/);
  assert.match(page, /splash-blob-sky/);
  assert.match(page, /splash-easy/);
  assert.match(page, /routineez-checklist\.png/);
  assert.match(page, /function EasyWord/);
  assert.match(page, /easy-e/);
  assert.match(page, /easy-a/);
  assert.match(page, /easy-s/);
  assert.match(page, /easy-y/);
  assert.match(page, /<span className="easy-a">A<\/span>/);
  assert.match(page, /<span className="easy-s">S<\/span>/);
  assert.match(page, /<span className="easy-y">Y<\/span>/);
  assert.match(page, /window\.clearTimeout\(splashTimer\)/);
  assert.match(page, /Edit routine/);
  assert.match(page, />Edit<\/button>/);
  assert.match(page, /name: form\.get\("name"\)/);
  assert.match(page, /emoji: form\.get\("emoji"\)/);
  assert.match(page, /color: form\.get\("color"\)/);
  assert.match(page, /Daily amount/);
  assert.match(page, /quantity-completions/);
  assert.match(page, /collapsed-progress/);
  assert.match(page, /add-modal-backdrop/);
  assert.match(page, /createPortal\(<div className="add-modal-backdrop routine-builder-page"/);
  assert.match(page, /aria-modal="true" aria-label="Add a routine"/);
  assert.match(page, /className="app-page-back routine-builder-back"/);
  assert.match(page, /className="app-page-back template-page-back"/);
  assert.match(page, /className="app-page-back settings-back"/);
  assert.match(page, /className="app-page-back history-page-back"/);
  assert.match(page, /aria-label=\{step === 0 \? "Close routine builder" : "Go back to previous step"\}/);
  const addRoutineSource = page.slice(page.indexOf("function AddRoutineForm"), page.indexOf("function UniqueChoiceToggles"));
  assert.doesNotMatch(addRoutineSource, /step === 0 \? "Cancel" : "Back"/);
  const fullPageBuilderCss = await readFile(new URL("../app/globals.css", import.meta.url), "utf8");
  assert.match(fullPageBuilderCss, /\.splash-blob > i::after/);
  assert.match(fullPageBuilderCss, /splash-glow-breathe/);
  assert.match(fullPageBuilderCss, /The add flow is a full-screen page/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page\.add-modal-backdrop \{[^}]*z-index: 220;[^}]*padding: 0;[^}]*overscroll-behavior: none;[^}]*backdrop-filter: none/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.add-routine-modal \{[^}]*width: 100%;[^}]*height: 100%;[^}]*overscroll-behavior-y: none;[^}]*-webkit-overflow-scrolling: auto;[^}]*border-radius: 0/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.routine-wizard-header \{[^}]*z-index: 5;[^}]*padding: max\(24px, env\(safe-area-inset-top\)\) 24px 20px;[^}]*background: #fff/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.wizard-heading \{[^}]*justify-items: center;[^}]*text-align: center/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.wizard-heading h2 \{[^}]*font-size: 24px;[^}]*font-weight: 760;[^}]*line-height: 1\.05/);
  assert.match(fullPageBuilderCss, /\.routine-builder-back \{[^}]*width: 42px;[^}]*height: 42px;[^}]*border: 1px solid color-mix[^}]*border-radius: 13px;[^}]*background: color-mix[^}]*box-shadow: 0 7px 17px/);
  assert.match(fullPageBuilderCss, /\.history-edit-page \.history-page-back \{[^}]*width: 42px;[^}]*height: 42px;[^}]*border-radius: 13px;[^}]*box-shadow: 0 7px 17px/);
  assert.match(fullPageBuilderCss, /\.settings-back \{[^}]*width: 40px;[^}]*height: 40px;[^}]*border-radius: 12px;[^}]*box-shadow: 0 7px 17px/);
  assert.match(fullPageBuilderCss, /One exact page-back control/);
  assert.match(fullPageBuilderCss, /button\.app-page-back\.app-page-back \{\s*width: 42px;\s*height: 42px;\s*min-width: 42px;\s*min-height: 42px;[^}]*border-radius: 13px;/);
  assert.match(fullPageBuilderCss, /\.today-page \.progress-card \{[^}]*margin-bottom: 14px;[^}]*box-shadow: none;/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.wizard-progress \{[^}]*width: 100%;[^}]*margin: 17px auto 0/);
  assert.match(fullPageBuilderCss, /\.routine-builder-page \.modal-scrollbar \{ z-index: 4; \}/);
  assert.doesNotMatch(fullPageBuilderCss, /routine-wizard-header \{[^}]*padding:[^;}]*66px/);
  assert.doesNotMatch(page, /A new small promise/);
  assert.match(page, /openAddFromHeader/);
  assert.match(page, /function useAnimatedNumber/);
  assert.match(page, /requestAnimationFrame\(animate\)/);
  assert.match(page, /setTimeout\(\(\) =>/);
  assert.doesNotMatch(page, /useMemo\(\(\) => new Date\(\), \[\]\)/);
  assert.match(page, /useState\(\(\) => new Date\(2000, 0, 1, 12\)\)/);
  assert.match(page, /setToday\(currentDate\)/);
  assert.doesNotMatch(page, /Different plan by day/);
  assert.match(page, /dayVariant-/);
  assert.match(page, /title: "Plan by day"/);
  assert.match(page, /function DayPlanSettings/);
  const addAppearanceSource = page.slice(page.indexOf('<section className="wizard-step" hidden={step !== 3} aria-label="Appearance">'), page.indexOf("function UniqueChoiceToggles"));
  assert.ok(addAppearanceSource.indexOf("<UniqueChoiceToggles") < addAppearanceSource.indexOf("<IconPicker"));
  const editAppearanceStart = page.lastIndexOf('<section className="wizard-step" hidden={step !== 3} aria-label="Appearance">');
  const editAppearanceSource = page.slice(editAppearanceStart, page.indexOf("function TrackingBuilder"));
  assert.ok(editAppearanceSource.indexOf("<UniqueChoiceToggles") < editAppearanceSource.indexOf("<IconPicker"));
  assert.match(fullPageBuilderCss, /\.choice-uniqueness \{[^}]*padding: 0;[^}]*border: 0;[^}]*background: transparent/);
  assert.match(fullPageBuilderCss, /\.choice-uniqueness-grid \{[^}]*grid-template-columns: repeat\(2, minmax\(0, 1fr\)\)/);
  assert.doesNotMatch(fullPageBuilderCss, /\.choice-toggle \+ \.choice-toggle \{/);
  assert.doesNotMatch(fullPageBuilderCss, /html\[data-theme="dark"\] :is\([^)]*\.choice-toggle:hover/);
  assert.match(page, /Choose a day to customize/);
  assert.doesNotMatch(page, /Edit repeat days/);
  assert.doesNotMatch(page, /day-plan-edit-toggle/);
  assert.match(page, /A day with no trackers is not used/);
  assert.match(page, /Uncheck everything to leave this day unused/);
  assert.match(page, /const toggleAllTracking = \(\) => saveTracking/);
  assert.match(page, /const next = activeTracking\.includes\(ref\)/);
  assert.match(page, /className="day-plan-days choosing"/);
  assert.match(fullPageBuilderCss, /\.day-plan-days \{[^}]*padding: 6px;[^}]*margin: -6px 0;/);
  assert.doesNotMatch(page, /className="day-plan-tabs"/);
  const dayPlanSource = page.slice(page.indexOf("function DayPlanSettings"), page.indexOf("function LoadingRows"));
  assert.doesNotMatch(dayPlanSource, /label: "Simple check"/);
  assert.doesNotMatch(dayPlanSource, />All tracking</);
  assert.match(dayPlanSource, /className="day-plan-select-all"/);
  assert.match(dayPlanSource, /"Clear all" : "Remove day"/);
  assert.match(page, /list:\$\{list\.key\}/);
  assert.match(page, /amount:\$\{amount\.key\}/);
  assert.match(page, /function routineTrackingForDay/);
  assert.doesNotMatch(page, /useSimple \? \[\]/);
  assert.match(page, /routineTrackingForDate\(routine, date\)/);
  assert.match(fullPageBuilderCss, /\.wizard-progress \{[^}]*repeat\(4, 1fr\)/);
  assert.match(fullPageBuilderCss, /\.add-form-shell\.step-tone-4 \{[^}]*var\(--sky\)/);
  assert.doesNotMatch(page, /Your rhythm at a glance/);
  assert.match(page, /createPortal\(<div className="add-modal-backdrop routine-builder-page edit-routine-page"/);
  assert.match(page, /aria-label="Edit routine progress"/);
  assert.match(page, /aria-label=\{step === 0 \? "Close routine editor" : "Go back to previous step"\}/);
  assert.match(page, /showTemplatePicker \|\| showAdd \|\| editingRoutineId !== null/);
  assert.match(page, /edit-routine-wizard/);
  const routineEditorSource = page.slice(page.indexOf("function RoutineOptionsEditor"), page.indexOf("function ScrollablePicker"));
  assert.doesNotMatch(routineEditorSource, /edit-modal-backdrop|edit-modal-stack|edit-form-shell|edit-routine-modal/);
  assert.doesNotMatch(routineEditorSource, /className="secondary-button"/);
  assert.match(page, /function DeleteRoutineDialog/);
  assert.match(page, /role="alertdialog"/);
  assert.match(page, /Delete routine/);
  assert.match(fullPageBuilderCss, /\.delete-dialog \{[^}]*box-shadow: 0 28px 70px rgba\(34,25,48,\.25\);/);
  assert.doesNotMatch(fullPageBuilderCss, /\.delete-dialog \{[^}]*inset 0 1px 0 #fff/);
  assert.match(page, /setRoutineToDelete\(routine\)/);
  assert.doesNotMatch(page, /onDelete=\{\(\) => deleteRoutine\(routine\.id\)\}/);
  assert.match(page, /Active dates/);
  assert.match(page, /name="startDate"/);
  assert.match(page, /name="endDate"/);
  assert.match(page, /function TimeField/);
  assert.match(page, /onClick=\{\(\) => updateTime\(""\)\}/);
  assert.match(page, /routine-live-preview/);
  assert.match(page, /function RoutineLivePreview/);
  assert.doesNotMatch(page, /<RoutineLivePreview/);
  assert.match(page, /Interactive routine preview/);
  assert.match(page, /preview-quantity/);
  assert.match(page, /preview-list/);
  assert.doesNotMatch(page, /filter\(Boolean\)\.slice\(0, 8\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-live-preview \{[^}]*flex: 0 0 auto/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /preview-list \{ overflow: visible; \}/);
  assert.match(page, /\+ Add tracking/);
  assert.match(page, /Named steps to check off/);
  assert.match(page, /Pause and resume one running total/);
  assert.match(page, /kind: "instructions", symbol: "≡", label: "Instructions"/);
  assert.match(page, /Save information or steps to follow/);
  assert.match(page, /not a daily entry/);
  assert.match(page, /function InstructionsPanel/);
  const instructionsPanelSource = page.slice(page.indexOf("function InstructionsPanel"), page.indexOf("function InstructionImageView"));
  assert.doesNotMatch(instructionsPanelSource, /aria-hidden="true">≡/);
  assert.doesNotMatch(page, />Bullet points</);
  assert.match(page, /className="instruction-rich-editor"/);
  assert.match(page, /aria-label="Bold"/);
  assert.match(page, /aria-label="Italic"/);
  assert.match(page, /aria-label="Bullet list"/);
  assert.match(page, /aria-label="Insert image"/);
  assert.match(page, /contentEditable role="textbox"/);
  assert.match(page, /pendingInstructionFiles/);
  assert.match(page, /pendingId/);
  assert.match(page, /function InstructionImageView/);
  assert.match(page, /function EnlargeablePhoto/);
  assert.match(page, /aria-label={`Enlarge \$\{alt\}`}/);
  assert.match(page, /className="photo-lightbox"/);
  assert.match(page, /className="photo-lightbox-close"/);
  assert.match(page, /<X aria-hidden="true" \/>/);
  assert.match(page, /uploadInstructionImages/);
  assert.match(deviceStorage, /saveDeviceInstructionImage/);
  assert.match(instructionImageRoute, /UPLOADS\.put/);
  assert.match(instructionImageRoute, /Instructions can include up to 6 images/);
  assert.match(instructionImageRoute, /20 \* 1024 \* 1024/);
  assert.match(instructionImageRoute, /export async function GET/);
  assert.match(page, /activeInstructions\.length > 0/);
  assert.match(page, /trackerKind\(tracker\) !== "instructions"/);
  assert.match(page, /Attach a daily progress image/);
  assert.match(page, /function TrackerControl/);
  assert.match(page, /className="tracker-photo-picker"/);
  assert.match(page, /aria-label="Remove photo"/);
  assert.match(page, /<Camera aria-hidden="true"/);
  assert.match(page, /<Trash2 aria-hidden="true"/);
  assert.match(page, /function optimizeImageForBrowserUpload/);
  assert.match(page, /MAX_BROWSER_IMAGE_UPLOAD_BYTES = 850_000/);
  assert.match(page, /preparedFile = await optimizeImageForBrowserUpload\(file\)/);
  assert.match(page, /response\.status === 413/);
  assert.match(page, /result\.error \|\| "Upload failed"/);
  assert.match(fullPageBuilderCss, /\.tracker-photo-picker input \{[^}]*inset: 0;[^}]*width: 100%;[^}]*height: 100%/);
  assert.match(fullPageBuilderCss, /\.tracker-photo img \{[^}]*width: 100%;[^}]*height: auto;[^}]*object-fit: contain/);
  assert.match(fullPageBuilderCss, /\.photo-lightbox \{[^}]*position: fixed;[^}]*z-index: 10000/);
  assert.doesNotMatch(page, /Already included on every routine/);
  assert.match(page, /capture="environment"/);
  assert.match(page, /I avoided this today/);
  assert.match(page, /Enter any amount for the day—there is no goal to reach/);
  assert.doesNotMatch(page, /Record minutes toward a goal/);
  assert.match(page, /value === "" \? 0 : Number\(value\) \|\| 0/);
  assert.doesNotMatch(page, /<button type="submit">Save<\/button>/);
  assert.match(page, /tracker-note-heading/);
  assert.doesNotMatch(page, />Save note<\/button>/);
  assert.match(page, /\.tracking-block input:invalid, \.tracking-block textarea:invalid/);
  assert.doesNotMatch(page, /<small>Live preview<\/small>/);
  assert.match(page, /className="preview-progress" role="progressbar"/);
  assert.doesNotMatch(page, /className="preview-detail-heading"/);
  assert.doesNotMatch(page, /className="quantity-caption"/);
  assert.match(page, /"simple" \| "checklist" \| "quantity" \| "hybrid"/);
  assert.match(page, /const usesChecklist/);
  assert.match(page, /const usesQuantity/);
  assert.match(page, /function TrackingBuilder/);
  assert.match(page, /trackingModeFor\(lists, amounts\)/);
  assert.match(page, /tracking-list-block/);
  assert.match(page, /tracking-amount-block/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /tracking-add-menu \{ position: absolute/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /tracking-add-button \{ width: 100%/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /step-tone-2 \{ --wizard-accent: var\(--coral\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /setting-card:nth-child\(3n \+ 2\) \{ --setting-accent: var\(--sky\)/);
  assert.match(page, /usesChecklist\(tracking\.mode\)[\s\S]*usesQuantity\(tracking\.mode\)/);
  assert.match(routinesRoute, /mode === "hybrid"/);
  assert.match(routinesRoute, /function cleanDayVariants/);
  assert.match(routinesRoute, /plan\.tracking/);
  assert.match(routinesRoute, /function cleanLists/);
  assert.match(routinesRoute, /Add at least one named list with an item/);
  assert.match(routinesRoute, /list_config AS listConfig/);
  assert.match(storage, /list_config TEXT NOT NULL DEFAULT '\[\]'/);
  assert.match(storage, /list_key TEXT NOT NULL DEFAULT 'list-1'/);
  assert.match(storage, /time_section TEXT NOT NULL DEFAULT 'auto'/);
  assert.match(storage, /sort_order INTEGER NOT NULL DEFAULT 0/);
  assert.match(storage, /idx_routines_owner_sort_order/);
  assert.match(routinesRoute, /export async function PATCH/);
  assert.match(routinesRoute, /ORDER BY sort_order, id/);
  assert.match(routinesRoute, /UPDATE routines SET time_section = \?, sort_order = \?/);
  assert.match(quantityRoute, /amount_key AS amountKey|amount_key = \?/);
  assert.match(quantityRoute, /INSERT INTO amount_completions/);
  assert.match(quantityRoute, /kind === "instructions"/);
  assert.match(routinesRoute, /amount_config AS amountConfig/);
  assert.match(routinesRoute, /requestedKind === "instructions"/);
  assert.match(routinesRoute, /record\.content/);
  assert.match(routinesRoute, /function cleanInstructionContent/);
  assert.match(routinesRoute, /version: 1, blocks/);
  assert.match(dataRoute, /"instructions"/);
  assert.match(dataRoute, /item\.content/);
  assert.match(fullPageBuilderCss, /\.routine-instruction \{/);
  assert.match(fullPageBuilderCss, /\.routine-instruction \{[^}]*padding: 0;[^}]*border: 0;[^}]*background: transparent/);
  assert.match(fullPageBuilderCss, /\.instruction-rich-toolbar \{/);
  assert.match(fullPageBuilderCss, /\.instruction-rich-content \{[^}]*min-height: 180px/);
  assert.match(fullPageBuilderCss, /\.instruction-rich-content ul \{ list-style: disc outside; \}/);
  assert.match(fullPageBuilderCss, /\.instruction-rich-content li \{ display: list-item/);
  assert.match(fullPageBuilderCss, /\.routine-instruction ul \{[^}]*list-style: none/);
  assert.match(fullPageBuilderCss, /\.routine-instruction li::before \{[^}]*content: "•"/);
  assert.doesNotMatch(page, /aria-label="Font size"/);
  assert.match(fullPageBuilderCss, /\.routine-instruction-inline-image img \{[^}]*object-fit: contain/);
  assert.match(storage, /CREATE TABLE IF NOT EXISTS amount_completions/);
  assert.match(page, /setPreviewEmoji/);
  assert.match(page, /setPreviewColor/);
  assert.match(page, /function VerticalScrollIndicator/);
  assert.match(page, /setScrollable\(maxScroll > 1\)/);
  assert.match(page, /if \(!scrollable\) return null/);
  assert.match(page, /mutationObserver/);
  assert.match(page, /Scroll \$\{label\}/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /modal-scrollbar::before/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /modal-scroll-thumb::after[\s\S]*width: 5px[\s\S]*background: var\(--wizard-accent, var\(--sky\)\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /html\[data-theme="dark"\] \.modal-scrollbar::before[\s\S]*#4a464e/);
  assert.match(page, /picker-scroll/);
  assert.match(page, /function ScrollablePicker/);
  assert.match(page, /<IconPicker availableEmojis=\{availableEmojis\}/);
  assert.match(page, /<ScrollablePicker label="Color" wrap>/);
  assert.match(page, /!wrap && scrollable &&/);
  assert.match(page, /wrap \? " picker-wrap"/);
  assert.match(page, /Calendar routine filters/);
  assert.match(page, /viewingCurrentMonth/);
  assert.match(page, /calendar-today-button/);
  assert.match(page, /calendar-detail-toolbar/);
  assert.match(page, /calendar-detail-card/);
  assert.doesNotMatch(page, /calendarScheduledDays|calendarRoutineCount|calendar-summary-stats|Days scheduled|Routines shown/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.calendar-page-matched \{ display: flex; flex-direction: column; padding-bottom: 88px; overflow: hidden/);
  assert.match(page, /calendar-month-heading/);
  assert.doesNotMatch(page, /calendar-today-row/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /history-month-toolbar \.calendar-today-button \{[^}]*color: var\(--sky\)[^}]*background: color-mix/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /@keyframes calendar-today-arrive/);
  assert.match(page, /setMonth\(new Date\(today\.getFullYear\(\), today\.getMonth\(\), 1\)\)/);
  assert.match(page, /calendar-filter-picker/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /@property --easy-ring-angle[\s\S]*?@keyframes easy-ring-orbit/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /:is\(\.calendar-filter-picker, \.history-filter-picker\) \.filter-pills button:first-child\.active \{[\s\S]*?border: 3px solid transparent;[\s\S]*?linear-gradient\(#fff,#fff\) padding-box,[\s\S]*?conic-gradient\(from var\(--easy-ring-angle\), var\(--purple\) 0 25%, var\(--coral\) 25% 50%, var\(--gold\) 50% 75%, var\(--sky\) 75% 100%\) border-box;[\s\S]*?box-shadow: none;[\s\S]*?animation: easy-ring-orbit 4\.8s linear infinite/);
  assert.match(page, /scrollClassName="filter-pills"/);
  assert.match(page, /accent=\{selectedCalendarRoutine\?\.color\} multicolor=\{selectedRoutine === "all"\}/);
  assert.match(page, /accent=\{selected\?\.color\} multicolor=\{effectiveSelection === "all"\}/);
  assert.match(page, /Scroll horizontally for more/);
  assert.doesNotMatch(page, /scroller\.scrollBy|picker-scroll-hint/);
  assert.match(page, /picker-scrollbar/);
  assert.match(page, /scrollFromThumb/);
  assert.match(page, /setPointerCapture/);
  assert.match(page, /Scroll \$\{label\} choices/);
  assert.match(page, /ResizeObserver/);
  assert.match(page, /boundedScroll/);
  assert.match(page, /calc\(\$\{thumb\.left\}% \+ 1px\)/);
  assert.match(page, /keepModalAligned/);
  assert.match(page, /modal\.scrollLeft = 0/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /picker-thumb::after/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /emoji-picker input, \.color-picker input \{ position: absolute; inset: 0/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /picker-scrollbar \{[\s\S]*width: 100%; height: 16px/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /picker-scrollbar::before \{[\s\S]*height: 3px[\s\S]*transform: translateY\(-50%\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /picker-thumb::after \{[\s\S]*height: 5px[\s\S]*border-radius: 3px[\s\S]*background: var\(--picker-accent, var\(--mint\)\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /picker-shell\.picker-multicolor \.picker-thumb::after \{[\s\S]*linear-gradient\(90deg, var\(--purple\) 0 25%, var\(--coral\) 25% 50%, var\(--gold\) 50% 75%, var\(--sky\) 75% 100%\)/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.picker-thumb \{[^}]*transition:[^;}]*left/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.picker-scroll \{[^}]*scroll-behavior: auto;[^}]*scroll-snap-type: none/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.picker-scroll\.picker-wrap \{[^}]*display: grid;[^}]*overflow: visible/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.emoji-picker \.picker-wrap \{[^}]*repeat\(auto-fill, 34px\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.color-picker \.picker-wrap \{[^}]*repeat\(auto-fill, 25px\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.emoji-picker label > span/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.color-picker label > span/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.emoji-picker span \{/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.color-picker span \{/);
  assert.match(page, /#8338EC/);
  assert.match(page, /🪥/);
  assert.match(page, /#00A896/);
  assert.match(page, /#212529/);
  assert.match(page, /#CC5DE8/);
  assert.match(page, /🏊/);
  assert.match(page, /🧩/);
  assert.match(page, /👨‍👩‍👧‍👦/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routines-page \{ scrollbar-width: none/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.app-background-blobs \.splash-blob \{ animation-name: app-blob-pop/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.splash-overlay\.leaving \{ visibility: hidden; opacity: 0/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-row\.completed \{ opacity: 1/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /today-page \.routine-list \{[^}]*grid-auto-rows: max-content;[^}]*overflow-y: auto/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.routine-row \{ min-height: 110px/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-row:hover, \.routine-row:active \{ transform: none/);
  assert.match(page, /className="sidebar-date"/);
  assert.match(page, /weekday: "long", month: "long", day: "numeric"/);
  assert.doesNotMatch(page, /desktop-calendar-icon/);
  assert.doesNotMatch(page, /desktop-today-date/);
  assert.match(page, /function CalendarNavButton/);
  assert.match(page, /date-nav-icon/);
  assert.match(page, /month: "short"/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /calendar-nav-button\.active \{ position: relative;[\s\S]*background: transparent; border: 0/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /mobile-wordmark img \{ width: 38px[^}]*drop-shadow\(0 0 13px rgba\(32,34,44/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /mobile-wordmark img \{[^}]*transform: translateY\(2px\)/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /mobile-wordmark img \{[^}]*border:/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav \{[\s\S]*height: 64px;[\s\S]*border-radius: 20px/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav button\.active::after \{ display: none/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /calendar-page, \.routines-page, \.history-page \{ padding-bottom: 132px/);
  assert.match(page, /function BottomNavSurface/);
  assert.match(page, /key=\{`\$\{tab\}-\$\{bottomNavPhase === "exiting" \? "closing" : "opening"\}`\}[\s\S]*?phase=\{bottomNavPhase\}/);
  assert.match(page, /today: \{ percent: "12\.5%", offset: 9 \}[\s\S]*?history: \{ percent: "87\.5%", offset: -9 \}/);
  assert.match(page, /bottom-nav-notch-\$\{tab\}/);
  assert.match(page, /M 42 -8 L -42 -8 L -42 0 C -36 0 -34 2 -33 9/);
  assert.match(page, /<g transform=\{`translate\(\$\{center\.offset\} 0\)`\}>[\s\S]*?bottom-nav-notch-motion\$\{notchMotionClass\}[\s\S]*?<use href=\{`#\$\{notchId\}`\} x=\{center\.percent\} y="0" fill="#000"/);
  assert.match(page, /const notchMotionClass = reducedMotion \? "" : closing \? " bottom-nav-notch-closing" : " bottom-nav-notch-opening"/);
  assert.match(page, /<linearGradient id=\{sheenId\}[\s\S]*?bottom-nav-sheen-bottom/);
  assert.match(page, /className="bottom-nav-surface-sheen"/);
  assert.match(page, /className="mobile-add premium-action"/);
  assert.doesNotMatch(page, /className="bottom-nav-add"|className="mobile-header-balance"/);
  assert.doesNotMatch(page, /LiquidButton|liquid-glass/);
  assert.match(mobileIndex, /maximum-scale=1, minimum-scale=1, user-scalable=no, viewport-fit=cover/);
  assert.match(mobileMain, /document\.documentElement\.classList\.add\("capacitor-native"\)/);
  assert.match(capacitorConfig, /contentInset: "never"/);
  assert.match(fullPageBuilderCss, /html\.capacitor-native body \{ position: fixed; inset: 0; touch-action: manipulation/);
  assert.match(fullPageBuilderCss, /html\.capacitor-native input,[\s\S]*?font-size: max\(16px, 1em\)/);
  assert.match(page, /premium-action/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /Premium actions: solid, polished/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /--brand-spectrum/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /App-icon palette: one coordinated system/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav button:nth-of-type\(1\)\.active[^}]*color: var\(--purple\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav button:nth-of-type\(2\)\.active[^}]*color: var\(--coral\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /grid-template-columns: repeat\(4, minmax\(0, 1fr\)\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav button:nth-of-type\(3\)\.active[^}]*color: #b97a08/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /bottom-nav button:nth-of-type\(4\)\.active[^}]*color: var\(--sky\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.premium-action \{\s*background: var\(--sky\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.easy-e \{ color: var\(--purple\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.easy-a \{ color: var\(--coral\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.easy-s \{ color: var\(--gold\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.easy-y \{ color: var\(--sky\)/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-detail-card \{ padding: 0; border: 0; border-radius: 0; background: transparent; box-shadow: none; \}/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-detail-card > header \{ display: grid; justify-items: center;/);
  assert.match(page, /history-page-detail/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-page-detail \{ display: flex; flex-direction: column; padding-bottom: 88px; overflow: hidden/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-page-detail \.history-day-spacer, \.history-page-detail \.history-day \{ height: clamp\(34px, 5svh, 44px\); aspect-ratio: auto/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-legend \{ display: flex; align-items: center; justify-content: center/);
  const monthlyHistory = page.slice(page.indexOf('return <section className="history-detail-card"'), page.indexOf('})() : <section className="history-overview"'));
  assert.doesNotMatch(monthlyHistory, /Completion history|at a glance/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /Calm typography shared by Settings/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.section-title h2, \.calendar-toolbar h2[^}]*font-weight: 600/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.primary-button, \.secondary-button[^}]*font-weight: 650/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /@keyframes routine-reveal/);
  assert.match(page, /CalendarPlus2/);
  assert.doesNotMatch(page, /onboarding-reference-scene|onboarding-skip|Build my first routine/);
  assert.match(page, /ChevronLeft/);
  assert.match(page, /day-fill/);
  assert.match(page, /calendar-day \$\{isToday/);
  assert.doesNotMatch(page, /day-dots/);
  assert.doesNotMatch(page, /empty-state-icon[^>]*>☀️/);
  assert.match(page, /mobile-wordmark/);
  assert.match(page, /routineez-checklist\.png/);
  assert.doesNotMatch(page, /src="\/routineez-logo\.png"/);
  assert.doesNotMatch(page, /routineez-mark\.png/);
  assert.match(layout, /const favicon = "\/routine-easy-splash-icon-v3\.png\?v=20260818-3"/);
  assert.match(layout, /const faviconFallback = "\/favicon\.ico\?v=20260818-3"/);
  assert.match(layout, /icons:[\s\S]*icon: \[[\s\S]*url: favicon[\s\S]*url: faviconFallback/);
  assert.doesNotMatch(layout, /routineez-favicon\.png/);
  assert.match(page, /mobile-profile/);
  assert.match(page, /className="delete-button"[\s\S]*?<Trash2 aria-hidden="true"/);
  assert.match(page, /Step \{step \+ 1\} of \{steps\.length\}/);
  assert.match(page, /The basics/);
  assert.match(page, /How to track it/);
  assert.match(page, /aria-label="Tracking options"/);
  assert.doesNotMatch(page, /<legend>Tracking<\/legend>|No extras yet|tracking-empty-note/);
  assert.match(page, /!lists\.length && !amounts\.length && <p className="tracking-empty-hint">Choose what you want to check off, count, time, or record\.<\/p>/);
  assert.match(page, /Make it yours/);
  assert.match(page, /Don’t reuse icons/);
  assert.match(page, /Don’t reuse colors/);
  assert.match(page, /availableEmojis/);
  assert.match(page, /availableColors/);
  assert.match(page, /const ICON_CATEGORIES = \[/);
  assert.match(page, /id: "health", label: "Health"/);
  assert.match(page, /id: "food", label: "Food"/);
  assert.match(page, /function IconPicker/);
  assert.match(page, /const ALL_ICON_PREVIEW_COUNT = 48/);
  assert.match(page, /const EASY_CATEGORY_COLORS = \["var\(--purple\)", "var\(--coral\)", "var\(--gold\)", "var\(--sky\)"\]/);
  assert.match(page, /role="tablist" aria-label="Icon categories"/);
  assert.match(page, /role="tabpanel"/);
  assert.match(page, /<IconPicker availableEmojis=\{availableEmojis\}/);
  assert.match(page, /className={`all-category\$\{category === "all" \? " active" : ""\}`}/);
  assert.match(page, /View more icons \(\$\{categoryEmojis\.length - ALL_ICON_PREVIEW_COUNT\}\)/);
  assert.match(page, /showAllIcons \? "Show less"/);
  assert.match(page, /"--category-color": EASY_CATEGORY_COLORS\[index % EASY_CATEGORY_COLORS\.length\]/);
  assert.match(page, /useLayoutEffect\(\(\) => \{[\s\S]*?grid\.getBoundingClientRect\(\)\.top[\s\S]*?previewEnd\.getBoundingClientRect\(\)\.bottom[\s\S]*?grid\.scrollHeight/);
  assert.match(page, /new ResizeObserver\(measure\)/);
  assert.match(page, /className={`emoji-grid-expander\$\{category === "all" \? " is-all" : ""\}\$\{showAllIcons \? " expanded" : ""\}`}/);
  assert.match(page, /inert=\{hiddenByCollapse \|\| undefined\}/);
  assert.match(page, /className="wizard-progress" role="progressbar"/);
  assert.match(page, /stepLockRef\.current/);
  assert.match(page, /aria-disabled=\{stepSettling\}/);
  assert.match(page, /disabled=\{saving\} aria-disabled=\{saving \|\| stepSettling\}/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /add-form-shell \{[^}]*flex: 1 1 auto; max-height: none; display: flex; overflow: hidden/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /add-modal-stack > \.routine-live-preview \{ max-height: min\(240px, 32svh\)/);
  assert.match(page, /CircleUserRound/);
  assert.match(page, /profile-popover-backdrop/);
  assert.match(page, /profile-settings-button/);
  assert.match(page, /profile-stats"><div><i><ListChecks/);
assert.match(page, /className="profile-routines-button"[\s\S]*?<ListChecks aria-hidden="true" \/>Manage routines<\/button>/);
  assert.match(page, /function SettingsPage/);
  assert.doesNotMatch(page, /<h2>Welcome screen<\/h2>|View onboarding|showOnboarding|onShowOnboarding/);
  assert.match(page, /settingsReturnTabRef = useRef<Exclude<Tab, "settings">>/);
  assert.match(page, /if \(tab !== "settings"\) settingsReturnTabRef\.current = tab/);
  assert.match(page, /className="settings-toolbar"/);
  assert.match(page, /className="app-page-back settings-back" onClick=\{onBack\} aria-label="Back to app"/);
  const settingsSource = page.slice(page.indexOf("function SettingsPage"), page.indexOf("function BlobCorners"));
  assert.doesNotMatch(settingsSource, /Make it yours|Choose how Routine EASY looks and feels/);
  assert.match(page, /tab === "settings" \? " settings-view-open"/);
  const settingsCss = await readFile(new URL("../app/globals.css", import.meta.url), "utf8");
  assert.match(settingsCss, /\.settings-view-open \.mobile-header \{ display: none; \}/);
  assert.match(settingsCss, /\.settings-view-open \.settings-page \{[^}]*width: 100%;[^}]*height: 100dvh/);
  assert.match(settingsCss, /\.settings-toolbar \{ position: fixed; inset: 0 0 auto;[^}]*backdrop-filter: blur\(18px\)/);
  assert.match(page, /routineez-preferences/);
  assert.match(page, /timeFormat: "12-hour"/);
  assert.match(page, /formatRoutineTime/);
  assert.match(page, /completed \? 100 : progressParts\.length/);
  assert.match(page, /!expanded && <div className="collapsed-progress"/);
  assert.match(page, /weekStartsOn/);
  assert.match(page, /preferences\.motion === "reduced"/);
  assert.match(page, /Manage routines/);
  assert.match(page, /className="settings-list" role="group" aria-label="Preferences"/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.setting-card \+ \.setting-card \{ border-top:/);
  assert.match(page, /Completed routines/);
  assert.match(page, /Sounds and haptics/);
  assert.match(page, /Get alerts at routine times while Routine EASY is open/);
  assert.match(page, /function DataPrivacyDialog/);
  assert.match(page, /routine-easy-backup-/);
  assert.match(page, /document\.documentElement\.dataset\.theme/);
  assert.match(page, /Notification\.requestPermission/);
  assert.match(dataRoute, /export async function GET/);
  assert.match(dataRoute, /export async function PUT/);
  assert.match(dataRoute, /export async function DELETE/);
  assert.match(dataRoute, /temporaryOwner/);
  assert.match(page, /function TemplateChooser/);
  const templateChooserSource = page.slice(page.indexOf("function TemplateChooser"), page.indexOf("function CompletionHistoryDialog"));
  assert.match(templateChooserSource, /return createPortal\(<div className="feature-dialog-backdrop routine-template-page"/);
  assert.match(templateChooserSource, /<BlobCorners className="creation-background-blobs" \/>/);
  assert.match(templateChooserSource, /<\/div>, document\.body\);/);
  assert.match(page, /className="template-dialog-header"/);
  assert.match(templateChooserSource, /className="app-page-back template-page-back"/);
  assert.match(templateChooserSource, /aria-label="Back to routines"/);
  assert.match(templateChooserSource, /className="template-options"/);
  assert.doesNotMatch(page, /className="template-heading-icon"/);
  assert.match(templateChooserSource, /<h2 id="template-title">Create routine<\/h2>/);
  assert.doesNotMatch(templateChooserSource, /How would you like to start\?|Choose a ready-made template or build your own|template-heading-copy/);
  assert.match(page, /Start from scratch/);
  assert.match(page, /ROUTINE_TEMPLATES/);
  const css = await readFile(new URL("../app/globals.css", import.meta.url), "utf8");
  assert.match(css, /\.icon-category-tabs \{[^}]*flex-wrap: wrap/);
  assert.match(css, /\.emoji-category-grid \{[^}]*repeat\(auto-fill, 34px\)/);
  assert.match(css, /\.emoji-grid-expander \{[^}]*transition: height \.42s cubic-bezier\(\.22,\.75,\.25,1\)/);
  assert.match(css, /\.emoji-grid-expander\.is-all:not\(\.expanded\) \.emoji-category-grid label:nth-child\(n \+ 49\)/);
  assert.match(css, /\.icon-category-tabs \.all-category,[^{]*\{[^}]*border: 3px solid transparent;[^}]*conic-gradient\(from var\(--easy-ring-angle\), var\(--purple\) 0 25%, var\(--coral\) 25% 50%, var\(--gold\) 50% 75%, var\(--sky\) 75% 100%\) border-box/);
  assert.match(css, /\.icon-category-tabs button\.active \{[^}]*background: var\(--category-color\)/);
  assert.match(css, /\.icon-view-more \{[^}]*border-radius: 999px/);
  assert.match(css, /\.add-form-shell\.step-tone-1 \{ --wizard-accent: var\(--purple\); \}/);
  assert.match(css, /\.add-form-shell\.step-tone-4 \{ --wizard-accent: var\(--sky\); --picker-accent: var\(--sky\); \}/);
  assert.match(css, /\.wizard-progress i:nth-child\(1\)\.active \{ background: var\(--purple\); \}/);
  assert.match(css, /\.wizard-progress i:nth-child\(4\)\.active \{ background: var\(--sky\); \}/);
  assert.match(css, /\.template-dialog > \.template-dialog-header \{[^}]*grid-template-columns: 46px minmax\(0, 1fr\) 36px/);
  assert.doesNotMatch(css, /\.template-dialog > \.template-dialog-header::after/);
  assert.match(css, /Template selection is the first page of the creation flow/);
  assert.match(css, /\.routine-template-page\.feature-dialog-backdrop \{[^}]*z-index: 220;[^}]*padding: 0;[^}]*backdrop-filter: none/);
  assert.match(css, /\.routine-template-page \.template-dialog \{[^}]*width: 100%;[^}]*height: 100svh;[^}]*border-radius: 0/);
  assert.match(css, /\.routine-template-page\.feature-dialog-backdrop \{[^}]*linear-gradient\(180deg, #fffdfa, #fbf8f2 72%\)/);
  assert.match(css, /\.creation-background-blobs \{ position: fixed; inset: 0; z-index: 0; \}/);
  assert.match(css, /\.creation-background-blobs \.splash-blob \{ opacity: \.42; animation: none; \}/);
  assert.match(css, /\.creation-background-blobs \.splash-blob > i \{ animation: none; \}/);
  assert.match(css, /\.routine-template-page\.feature-dialog-backdrop \{[^}]*animation: none/);
  assert.match(css, /\.routine-template-page \.template-options \{ animation: creation-content-in/);
  assert.match(page, /<div className="add-modal-backdrop routine-builder-page">\s*<BlobCorners className="creation-background-blobs" \/>/);
  assert.match(css, /\.routine-builder-page \.add-routine-modal \.form-actions \{[^}]*background: transparent;[^}]*backdrop-filter: none;[^}]*box-shadow: none/);
  assert.match(css, /html\[data-theme="dark"\] \.routine-builder-page \.add-routine-modal \.form-actions \{ background: transparent; box-shadow: none; \}/);
  assert.match(css, /\.routine-template-page \.template-dialog > \.template-dialog-header \{[^}]*height: calc\(68px \+ env\(safe-area-inset-top\)\);[^}]*grid-template-columns: 42px minmax\(0, 1fr\) 42px/);
  assert.match(page, /function animateBottomNavReturn/);
  assert.match(page, /function selectBottomTab\(nextTab: MainTab\)/);
  assert.doesNotMatch(page, /bottomNavPhase !== "idle"\) return/);
  assert.match(page, /setBottomNavPhase\("exiting"\)[\s\S]*?\}, 215\);/);
  assert.match(page, /bottom-nav nav-\$\{bottomNavPhase\}\$\{showTemplatePicker \|\| showAdd \|\| editingRoutineId !== null \|\| historyDayEditorOpen \|\| tab === "settings" \? " creation-flow-hidden" : bottomNavReturning \? " creation-flow-returning"/);
  assert.match(page, /function openSettings\(\) \{\s*prepareCreationFlow\(\);/);
  assert.match(page, /function closeSettings\(\) \{\s*setTab\(settingsReturnTabRef\.current\);\s*animateBottomNavReturn\(\);/);
  assert.match(page, /function openHistoryDayEditor\(\) \{\s*prepareCreationFlow\(\);\s*setHistoryDayEditorOpen\(true\);/);
  assert.match(page, /function closeHistoryDayEditor\(\) \{\s*setHistoryDayEditorOpen\(false\);\s*animateBottomNavReturn\(\);/);
  assert.match(page, /onClick=\{\(\) => openDayEditor\(day\)\}/);
  assert.match(css, /\.bottom-nav\.creation-flow-hidden \{ z-index: 230;/);
  assert.doesNotMatch(fullPageBuilderCss, /\.settings-view-open \.mobile-header, \.settings-view-open \.bottom-nav \{ display: none; \}/);
  assert.match(css, /@keyframes bottom-nav-slide-away/);
  assert.match(css, /@keyframes bottom-nav-shoot-up[\s\S]*?translateY\(-7px\)[\s\S]*?translateY\(3px\)/);
  assert.match(page, /function CompletionHistoryDialog/);
  assert.match(page, /function HistoryPage/);
  assert.match(page, /Your progress starts here/);
  assert.match(page, /onClick=\{onAddRoutine\}>Add your first routine/);
  assert.match(page, /!loading && !routines\.length \? " history-page-empty"/);
  assert.match(page, /\{routines\.length > 0 && <ScrollablePicker label="History routine filters"/);
  assert.match(page, /Day details/);
  assert.doesNotMatch(page, /Edit what was recorded for/);
  assert.match(page, /history-tracker-editor/);
  assert.match(page, /history-summary-fix/);
  assert.match(page, /history-status-editor/);
  assert.match(page, />Change status<\/span>/);
  assert.doesNotMatch(page, /Correct this day/);
  assert.match(page, /createPortal\(<div className="feature-dialog-backdrop history-edit-page">/);
  assert.match(page, /className="app-page-back history-page-back"[^>]*aria-label="Back to history"/);
  assert.match(page, /className="history-edit-sticky"/);
  assert.match(page, /Edit here · auto-saves/);
  assert.match(page, /history-record-row history-record-edit/);
  assert.match(page, /<TrackerControl routine=\{routine\} tracker=\{tracker\}/);
  assert.match(page, /prepareHistoryDetailEdit/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /\.history-edit-sticky \{ position: sticky/);
  assert.match(css, /\.history-edit-page\.feature-dialog-backdrop \{[^}]*z-index: 220;[^}]*padding: 0;[^}]*backdrop-filter: none/);
  assert.match(css, /\.history-edit-page \.history-edit-dialog \{[^}]*width: 100%;[^}]*height: 100svh;[^}]*border-radius: 0;[^}]*background: transparent/);
  assert.match(page, /History routine filters/);
  assert.match(page, /tab === "history"/);
  assert.match(page, /label="History"/);
  assert.match(page, /history-overview-grid/);
  assert.match(page, /<header><h2>Monthly progress<\/h2><\/header>/);
  assert.doesNotMatch(page, /Select a routine for the full monthly view/);
  assert.match(page, /className="history-overview-progress" role="progressbar"/);
  assert.match(page, /View monthly details/);
  assert.match(page, /className="history-overview-meta"/);
  assert.match(page, /history-title-row/);
  assert.match(page, /buildRoutineHistory/);
  assert.match(page, /Monthly completion/);
  assert.match(page, /history-month-toolbar/);
  assert.match(page, /history-current-month-button/);
  assert.match(page, /calendar-month-heading[^]*history-current-month-button/);
  assert.doesNotMatch(page, /This month/);
  assert.match(page, /className=\{`skipped[^]*?<SkipForward \/>/);
  assert.doesNotMatch(page, /↷/);
  assert.match(page, /Skip today/);
  assert.match(page, /Undo skip/);
  assert.match(page, /routine-swipe-surface/);
  assert.match(page, /setPointerCapture/);
  assert.match(page, /Math\.abs\(dragXRef\.current\) >= 70/);
  assert.doesNotMatch(page, /lastSwipeFinishedAtRef/);
  assert.doesNotMatch(page, /performance\.now\(\) - .*< 80/);
  assert.match(page, /const skippedRef = useRef\(skipped\)/);
  assert.match(page, /const nextSkipped = !skippedRef\.current/);
  assert.match(page, /onSkip\(nextSkipped\)/);
  assert.match(page, /routineMutationQueuesRef/);
  assert.match(page, /function queueRoutineMutation/);
  assert.match(page, /const previous = queues\.get\(routineId\) \?\? Promise\.resolve\(\)/);
  assert.match(page, /await setRoutineSkip\(routine\.id, false, date\)/);
  assert.match(page, /completionsRef\.current/);
  assert.doesNotMatch(page, /suppressClickRef/);
  assert.match(page, /role="button" tabIndex=\{0\}/);
  assert.match(page, /onPointerCancel=\{cancelSwipe\}/);
  assert.doesNotMatch(page, /!pointerActiveRef\.current \|\| expanded \|\| gestureAxisRef\.current === "vertical"/);
  assert.match(page, /const tapGesture = gestureAxis === "pending"/);
  assert.match(page, /Math\.abs\(dragXRef\.current\) < 18/);
  assert.match(page, /event\.key === "Enter" \|\| event\.key === " "/);
  assert.match(page, /function undoRoutineSkip/);
  assert.match(page, /className="routine-check-zone"/);
  assert.match(page, /const checkPointerRef = useRef/);
  assert.match(page, /onPointerDown=\{beginCheckPointer\}/);
  assert.match(page, /onPointerUp=\{finishCheckPointer\}/);
  assert.match(page, /distance < 22/);
  assert.doesNotMatch(page, /className="routine-check-zone" onClick=/);
  assert.match(page, /if \(skippedRef\.current\) toggleSkip\(\)/);
  assert.match(page, /else if \(hasDetails\) setExpanded/);
  assert.match(page, /Tap anywhere or swipe left or right to undo skip/);
  assert.match(page, /setAmount\(routine\.id, amount, 0, date, false\)/);
  assert.match(page, /aria-description=\{skipped/);
  assert.doesNotMatch(page, /className="skip-today-button"/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-row\.skipped \.collapsed-progress span \{ width: 100% !important/);
  assert.match(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-swipe-underlay/);
  assert.doesNotMatch(await readFile(new URL("../app/globals.css", import.meta.url), "utf8"), /routine-row\.skipped \{ border-style: dashed/);
  assert.match(page, /Duplicate/);
  const duplicateRoutineSource = page.slice(page.indexOf("function duplicateRoutine"), page.indexOf("async function deleteRoutine"));
  assert.match(duplicateRoutineSource, /setSelectedTemplate\(\{/);
  assert.match(duplicateRoutineSource, /setShowAdd\(true\)/);
  assert.doesNotMatch(duplicateRoutineSource, /fetch\(/);
  assert.match(page, /Save duplicate/);
  assert.match(completionRoute, /"completed" \| "skipped"/);
  assert.match(completionRoute, /ON CONFLICT\(owner_key, routine_id, date\) DO UPDATE SET status/);
  assert.match(routinesRoute, /routine_id AS routineId, date, status FROM completions/);
  assert.doesNotMatch(routinesRoute, /const defaults =|Morning vitamins|Warm up.*Main workout.*Cool down/s);
  assert.match(storage, /status TEXT NOT NULL DEFAULT 'completed'/);
  assert.doesNotMatch(page, /today-date-copy|date-balance/);
  assert.match(page, /item-completions/);
  assert.match(layout, /Routine EASY — Simple Routine Tracker/);
  assert.match(layout, /themeInitializationScript/);
  assert.match(layout, /routineez-preferences/);
  assert.match(layout, /suppressHydrationWarning/);
  assert.match(layout, /window\.matchMedia\("\(prefers-color-scheme: dark\)"\)/);
  assert.match(page, /if \(!preferencesLoaded\) return/);
  assert.doesNotMatch(page, /SkeletonPreview|codex-preview/);
  assert.doesNotMatch(packageJson, /react-loading-skeleton/);
});

test("dark mode covers mobile cards and interactive surfaces", async () => {
  const css = await readFile(new URL("../app/globals.css", import.meta.url), "utf8");

  assert.match(css, /Dark theme completion pass/);
  assert.match(css, /html\[data-theme="dark"\] \.progress-card \{[^}]*background: linear-gradient/);
  assert.match(css, /html\[data-theme="dark"\] :is\(\.check-circle, \.preview-check, \.preview-list button > span\)/);
  assert.match(css, /html\[data-theme="dark"\] \.bottom-nav \.date-nav-icon \{ background: #211e26/);
  assert.match(css, /html\[data-theme="dark"\] \.history-day \{ color: #aaa6b1; background: #211e26/);
  assert.match(css, /html\[data-theme="dark"\] :is\(\.tracking-block, \.tracking-add-menu > button:hover\)/);
  assert.doesNotMatch(css, /html\[data-theme="dark"\] :is\([^)]*\.choice-toggle:hover/);
  assert.match(css, /html\[data-theme="dark"\] \.picker-scrollbar::before/);
  assert.match(css, /html\[data-theme="dark"\] \.toggle-track \{ background: #4b4751/);
  assert.match(css, /html\[data-theme="dark"\] \.date-range-toggle \{[^}]*background: transparent;[^}]*box-shadow: none/);
  assert.match(css, /html\[data-theme="dark"\] \.date-range-settings \{ box-shadow: none; \}/);
  assert.match(css, /html\[data-theme="dark"\] \.filter-pills button\.active \{[^}]*background: var\(--pill\)/);
  assert.match(css, /html\[data-theme="dark"\] :is\(\.calendar-filter-picker, \.history-filter-picker\) \.filter-pills button:first-child\.active \{[\s\S]*?linear-gradient\(#2b2731,#2b2731\) padding-box,[\s\S]*?box-shadow: none/);
  assert.match(css, /html\[data-theme="dark"\] \.routine-card-actions button \{[^}]*background: color-mix/);
  assert.match(css, /html\[data-theme="dark"\] \.delete-button,/);
  assert.match(css, /Floating mobile navigation with a fully staged active-ball and pull-down cutout/);
  assert.doesNotMatch(css, /transition: --active-x|@property --active-x/);
  assert.match(css, /\.bottom-nav \{\s*inset: auto max\(14px, env\(safe-area-inset-right\)\) max\(12px, env\(safe-area-inset-bottom\)\) max\(14px, env\(safe-area-inset-left\)\);\s*height: 72px;[\s\S]*?grid-template-columns: repeat\(4/);
  assert.match(css, /\.bottom-nav \{[\s\S]*?background: transparent/);
  assert.match(css, /\.bottom-nav \{[\s\S]*?border-radius: 24px/);
  assert.match(css, /\.bottom-nav::before, \.bottom-nav::after \{ display: none/);
  assert.match(css, /\.bottom-nav-surface \{[\s\S]*?border-radius: 24px 24px 22px 22px/);
  assert.match(css, /\.bottom-nav-surface-fill \{ fill: #fff/);
  assert.match(css, /\.bottom-nav-surface-sheen \{ opacity: \.82/);
  assert.match(css, /drop-shadow\(0 0 \.75px rgba\(68,63,82,\.48\)\)/);
  assert.match(css, /@keyframes bottom-nav-active-pop[\s\S]*?translate\(-50%, -50%\) translateY\(-27px\) scale\(0\)[\s\S]*?scale\(1\)/);
  assert.match(css, /@keyframes bottom-nav-active-out[\s\S]*?translate\(-50%, -50%\) translateY\(-27px\) scale\(0\)/);
  assert.match(css, /@keyframes bottom-nav-notch-pull-down[\s\S]*?translateY\(-48px\)[\s\S]*?translateY\(0\)/);
  assert.match(css, /@keyframes bottom-nav-notch-pull-up[\s\S]*?translateY\(0\)[\s\S]*?translateY\(-48px\)/);
  assert.match(css, /\.bottom-nav-notch-opening \{[^}]*bottom-nav-notch-pull-down \.36s/);
  assert.match(css, /\.bottom-nav-notch-closing \{[^}]*bottom-nav-notch-pull-up \.21s/);
  assert.match(css, /@keyframes bottom-nav-icon-lift[\s\S]*?translateY\(-27px\)[\s\S]*?animation: bottom-nav-icon-lift \.27s \.035s/);
  assert.match(css, /\.bottom-nav\.nav-exiting button\.active::before[\s\S]*?bottom-nav-active-out \.16s/);
  assert.match(css, /\.bottom-nav \.nav-label \{[\s\S]*?top: 48px;[\s\S]*?color: currentColor;[\s\S]*?font-size: 9px/);
  assert.match(css, /\.bottom-nav button\.active \.nav-label,[\s\S]*?color: var\(--nav-accent\)/);
  assert.match(css, /\.bottom-nav button\.calendar-nav-button:nth-of-type\(2\) \{ --nav-accent: var\(--coral\)/);
  assert.match(css, /\.bottom-nav button:nth-of-type\(3\) \{ --nav-accent: var\(--gold\)/);
  assert.match(css, /\.bottom-nav button:nth-of-type\(3\)\.active \.nav-label \{ color: #a96b00/);
  assert.match(css, /\.calendar-nav-button\.active \.date-nav-icon \{[\s\S]*?color: #fff;[\s\S]*?border-color: rgba\(255,255,255,\.96\);[\s\S]*?background: #fff/);
  assert.match(css, /\.calendar-nav-button\.active \.date-nav-icon i \{ color: #fff; background: var\(--nav-accent\)/);
  assert.match(css, /\.calendar-nav-button\.active \.date-nav-icon strong \{ color: var\(--nav-accent\)/);
  assert.match(css, /\.bottom-nav button:focus-visible \{ outline: 0; \}[\s\S]*?outline-offset: 4px/);
  assert.doesNotMatch(css, /\.bottom-nav button\.bottom-nav-add \{/);
  assert.match(css, /\.bottom-nav button\.active::before,[\s\S]*?background: var\(--nav-accent\)/);
  assert.match(css, /\.bottom-nav button\.active \.nav-icon \{[\s\S]*?background: transparent/);
  assert.match(css, /html\[data-theme="dark"\] \.bottom-nav-surface-fill \{ fill: #211f25/);
  assert.match(css, /html\[data-theme="dark"\] \.bottom-nav-surface-sheen \{ opacity: \.4/);
});

test("motion polish stays tactile and respects reduced motion", async () => {
  const page = await readFile(new URL("../app/page.tsx", import.meta.url), "utf8");
  const css = await readFile(new URL("../app/globals.css", import.meta.url), "utf8");

  assert.match(css, /Motion polish: short, purposeful feedback/);
  assert.match(css, /\.page \{[^}]*animation: enter \.38s cubic-bezier\(\.16,1,\.3,1\)/);
  assert.match(css, /\.progress-track span::after \{[^}]*animation: progress-sheen \.9s \.3s/);
  assert.match(css, /\.filter-pills button:active \{ transform: translateY\(1px\) scale\(\.96\)/);
  assert.match(css, /\.calendar-month-view \.calendar-day\.has-routines \{ animation: calendar-detail-arrive/);
  assert.match(css, /\.routine-row:hover \.routine-emoji \{ transform: translateY\(-1px\) rotate\(-3deg\) scale\(1\.045\)/);
  assert.match(page, /className="routine-expansion" aria-hidden=\{!expanded\}/);
  assert.match(page, /aria-hidden=\{!expanded\} inert=\{!expanded\} style=\{\{ height: expanded \? `\$\{expansionHeight\}px` : "0px" \}\}/);
  assert.match(page, /const measure = \(\) => setExpansionHeight\(content\.scrollHeight\)/);
  assert.match(page, /new ResizeObserver\(measure\)/);
  assert.doesNotMatch(page, /activeItems\.length > 0 && expanded &&/);
  assert.match(css, /\.routine-expansion \{[^}]*height: 0;[^}]*overflow: hidden;[^}]*transition: height \.4s cubic-bezier\(\.22,\.75,\.25,1\)/);
  assert.doesNotMatch(css, /\.routine-expansion \{[^}]*opacity:/);
  assert.doesNotMatch(css, /\.routine-expansion \{[^}]*visibility:/);
  assert.doesNotMatch(css, /\.routine-row\.expanded \.routine-expansion \{[^}]*opacity:/);
  assert.doesNotMatch(css, /\.routine-checklist::before|\.quantity-trackers::before/);
  assert.match(css, /\.routine-swipe-surface \{[^}]*border: 0/);
  assert.match(css, /\.routine-expansion \{[^}]*border: 0/);
  assert.doesNotMatch(css, /\.quantity-trackers\.tracker-controls-collapsed \{ display: none/);
  assert.doesNotMatch(css, /\.routine-row:not\(\.expanded\) \{ height: 110px/);
  assert.match(css, /\.history-overview-card \{ animation: detail-card-arrive/);
  assert.match(css, /\.history-overview-progress \{[^}]*height: 6px;[^}]*border-radius: 999px/);
  assert.match(css, /\.history-overview-progress i \{[^}]*background: var\(--history-color\);[^}]*transition: width \.5s/);
  assert.match(css, /\.history-overview > header \{[^}]*justify-content: flex-start;[^}]*margin: 0 0 12px;[^}]*text-align: left/);
  assert.match(css, /\.setting-card:hover \.setting-icon \{ transform: translateY\(-1px\) rotate\(-4deg\) scale\(1\.055\)/);
  assert.match(css, /@keyframes saved-check-arrive/);
  assert.match(css, /\.reduce-motion \*, \.reduce-motion \*::before, \.reduce-motion \*::after \{[^}]*animation-iteration-count: 1 !important/);
  assert.match(css, /@media \(prefers-reduced-motion: reduce\) \{[\s\S]*?animation-iteration-count: 1 !important/);
});
